# 인수인계 — 새 세션은 이 문서부터 읽으세요

> 새 세션(클라우드/PC/폰)은 이 문서 + `CLAUDE.md`(작업 규칙)만 읽으면 맥락을 이어받습니다.
> 파이프라인·명령 전체 지도는 `README.md`.

## 프로젝트 한 줄
제이로그(jaylog.co.kr) — 해외직구·통관 블로그. **애드센스 승인 → 수익화**가 목표. **30~50대 타깃**(2026-08-19 50대→30~50대 확장).

## 현재 상태 (2026-08-10 기준)
- **라이브 글 23개**: 발행 22 + 예약 1(284 관세 과오납 환급, 8/11 20:35). 전부 카테고리 6(해외직구·통관 가이드).
- **GEO 100% 적용**: 23/23 글에 상단 '한눈 요약' + 하단 FAQ + FAQPage JSON-LD 완비
  (검증: 폰 명령 `geo-audit` → `review/geo-audit.txt`).
- **신뢰 페이지 완비**: 소개·문의·개인정보처리방침·이용약관 + 관부가세 계산기 전용 페이지.
- **완전 클라우드 파이프라인 가동 중** (PC 불필요):
  - `drafts/`에 초안 커밋 → 썸네일·본문이미지(OpenAI) + GEO 렌더 + WP 등록 + 하루 1개 예약
  - 이미지 생성 실패(크레딧 소진 등) 시에도 **글은 텍스트로 등록**되고 '썸네일필요' 표시 → 나중에 `add-images`로 보완
  - `wp-cron-ping.yml` 매시간 예약발행, `blog-command.yml` 폰 관리
- 시크릿 등록됨: `WP_APP_PASSWORD`, `OPENAI_API_KEY`.

## 최근 큰 작업 (2026-08-08~10)
- 애드센스 거절(획일감) 대응: 기존 글 전부 형식·도입·마무리를 서로 다르게 재작성(`revisions/`).
- **Orca(orca-ai-company)에서 GEO 구조 이식**: answer_summary·faq 필드 도입 → 신규 글 자동 적용 +
  기존 23개 전부 소급 백필. FAQPage JSON-LD는 wpautop 안 깨지게 한 줄 처리.
- 파이프라인 내구성 개선(이미지 실패 폴백), `add-images`/`geo-audit`/`backfill-geo` 도구 추가.
- `wordpress-snippets/llms-txt.php`: 답변엔진용 `/llms.txt` (WPCode에 붙이면 활성 — 아직 미적용, 선택).

## 다음 할 일
1. **하루 1개 발행 페이스 유지** — 주제 사전 허가제(후보 5개 제시 → 허가 → 작성 → 예약) 준수.
2. OpenAI 크레딧 유지(떨어지면 썸네일 자동 생략됨 → 충전 후 `add-images`로 보완).
3. 애드센스 재심사 상태 주시(획일감 개선 + GEO 반영 후).
4. 새 글은 프론트매터에 `answer_summary`·`faq` 반드시 채우기(파이프라인이 경고).

## 운영 원칙 (상세는 CLAUDE.md)
- 하루 1개 발행, main 브랜치에만 커밋, 비밀키 커밋 금지, 주제 사전 허가제, 유튜브 접속 금지.
- 글: 30~50대 타깃, 획일감 금지, 후킹 도입, h2 3~5개, 공백제외 1,700자+, 표/리스트, 요약·FAQ 필수, 핵심 강조(시인성).
- 썸네일: 다크네이비+골드 프리미엄, 한글 문구 정확히, 브랜드 로고 금지.

## 주의사항 (과거 사고 이력)
- 병렬 작업 시 임시파일 공용 이름 → 썸네일 섞임 사고 → 글번호 접두어 필수.
- 시간 판단은 서버 기준(로컬 시계 부정확 이력). 규칙 문서에 키 조각 절대 금지.
- `published/`의 `등록됨: post NNN` 주석은 등록 시점 기록 — 일부 옛 id(101·104·110·113 등)는
  현재 라이브에 없을 수 있음(초안/휴지통). 라이브 실집계는 `status`/`geo-audit`로 확인.
