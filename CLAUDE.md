# CLAUDE.md — 제이로그 작업 규칙 (모든 PC 공통)

이 저장소는 여러 PC와 폰(클라우드)에서 함께 작업한다. 어느 환경의 Claude든 아래 규칙을 따른다.

## 🔄 동기화 (가장 중요)
- **모든 작업은 main 브랜치에 직접 커밋한다.** 별도 브랜치·PR을 만들지 말 것 — 자동 파이프라인(publish-draft.yml)이 main만 감시하므로, 브랜치에 두면 썸네일·등록 자동화가 작동하지 않는다.
- **작업 시작할 때**: 반드시 `git pull origin main` 먼저 — 다른 곳에서 한 작업을 받아온다.
- **의미 있는 변경 후**: 즉시 커밋하고 `git push origin main` — 글계획/작성현황 갱신, 새 초안, 규칙 수정 등.
- 푸시 전 **비밀키 스캔 필수**: `secrets.local.md`에 있는 실제 키/비밀번호 문자열(또는 그 일부)이 커밋 내용에 들어있는지 확인하고, 있으면 중단. (스캔할 때 실제 값은 secrets.local.md에서 읽어서 대조 — 규칙 문서에 키 조각을 적지 말 것)
- `secrets.local.md`는 절대 커밋하지 않는다 (.gitignore에 있음).

## 📌 프로젝트 개요
- 블로그: https://jaylog.co.kr (워드프레스) — 해외직구·통관 정보
- 목표: 구글 애드센스 승인 → 수익화
- 글은 Claude가 REST API로 직접 작성·발행 (인증: `secrets.local.md`, 사용자 claude)
- 이 파일 외 상세: `README.md`, `wp-api-config.md`, `글계획.md`, `작성현황.md`

## ✍️ 글쓰기 규칙 (요약 — 상세는 agents/jaylog-writer.md)
- **타깃: 50대** 독자. 쉬운 말, 실생활 사례, 50대 관심 품목(건기식·골프·명품·의약품·여행 등).
- **⛔ 획일감 금지 (2026-08-08 애드센스 거절 후 최우선)**: 모든 글이 똑같은 뼈대로 찍어낸 느낌을 주면 안 된다. 도입·구조·마무리·고지문을 글마다 다르게, 필자 실무자 관점을 정직하게 1~2군데(없는 일화·수치 조작 금지). "정리하면"·"다음 글 예고" 상투구 금지. 상세는 writer 규칙.
- **계산기는 인라인 위젯 복붙 금지** → 계산기 전용 페이지(`https://jaylog.co.kr/gwanbuga-calculator/`)로 버튼 링크만. drafts의 `calculator:`가 none이 아니면 파이프라인이 자동 삽입.
- **GEO(답변엔진 인용) 필수**: 글마다 `answer_summary`(상단 한눈 요약)와 `faq`(3문항) 작성 — 파이프라인이 상단 요약 박스 + 하단 FAQ + FAQPage JSON-LD로 렌더. ChatGPT·Perplexity가 인용하게 만드는 핵심(Orca 구조 이식).
- **⛔ AI 티 제거(humanize) 필수**: 초안을 drafts/에 커밋하기 전 `/humanize-korean` 스킬로 한 번 훑어 AI 티(번역투 "~를 통해", 상투구 "결론적으로/시사하는 바", 기계적 "첫째·둘째·셋째", 피동·문두 접속사 남발)를 제거한다. **의미·수치는 절대 불변**. 발행 파이프라인이 `humanize_gate.py`로 AI 티를 자동 점수화해 경고하며, 경고 뜬 초안은 윤문 후 다시 커밋한다. (근거: katfishnet ACL 2025 — 한국어 AI 글은 이 패턴들로 탐지된다. 도구: im-not-ai, MIT.)
- 구조: 후킹 도입(뻔한 도입 금지) → h2 소제목 3~5개(그 자체로 정보가 되게) → 표/리스트 1개 이상 → 마무리. 공백 제외 1,700자 이상(2,000~2,600 권장).
- 카테고리: **6 (해외직구·통관 가이드) 단 하나만** 사용.
- 키워드(태그) 4~6개 + 발행글 내부링크 + 말미 고지문.
- 규정·수치는 2026년 기준 정확하게, 불확실하면 공식 출처 확인.
- 썸네일: OpenAI gpt-image-2, 고급 프리미엄 스타일(다크네이비+골드), 글당 1장, 한글 오타 육안 검수. (키 없는 환경이면 썸네일 생략하고 초안만 — "썸네일 필요" 표시 남기기)
- **본문 이미지: 글마다 1장 필수** — 글의 가장 중요한 핵심 대목에 어울리는 이미지를 OpenAI로 생성해 본문 중간에 삽입. 프리미엄 톤 유지, **한글 텍스트 금지**(오타 위험). 상세는 agents/jaylog-writer.md.

## 🚦 발행 정책
- **⛔ 주제 사전 허가제 (2026-08-03 지시, 08-04 재강조)**: 순서는 반드시 **① 후보 주제 5개 제시 → ② 사용자 허가 → ③ 글 작성 → ④ 예약**. 허가 없이는 글 작성·drafts 커밋·예약(schedule-queue 포함) 그 무엇도 먼저 하지 않는다. 예약만 먼저 걸어두는 것도 금지. (이미 허가받아 예약된 글은 그대로 진행)
- **하루 1개만 발행.** 여러 개는 초안으로 쌓고 예약발행(future)으로 분산, 발행 시간은 매일 다르게.
- 예약발행 누락은 GitHub Actions(`wp-cron-ping.yml`)가 매시간 깨워서 보완함.
- 폰/원격 발행·예약: GitHub Actions `blog-command.yml` (Run workflow → status / publish-one / schedule-queue).
- 글감 조사: GitHub Actions `kin-research.yml` (Run workflow → 키워드 입력) — 네이버 지식iN 질문 목록을 `research/`에 수집. 인증은 시크릿 NAVER_CLIENT_ID/SECRET.
- **지식iN 답글(블로그 홍보) — 미답변 질문에 답변 (2026-08-15 지시)**:
  - **⛔ 제목만 보고 답하지 말 것.** 반드시 `kin-fetch.yml`(docId 입력)로 **질문 원문을 가져와** 질문자가 실제로 뭘 궁금해하는지 파악한 뒤 그에 정확히 맞춰 답한다. (제목만 보면 엉뚱한 답이 나온다 — 실제 사례 있음: '디카 통관' 질문의 핵심은 세금이 아니라 전파인증 수량+같은날 입항이었음.)
  - 미답변 질문 수집은 `kin-unanswered.yml`(검색 API로 최근 질문→각 페이지 답변수 확인→답변 0개만). 답변된 질문·오래된 질문은 건너뛴다.
  - 답변은 그 자체로 도움이 되게 충실히, **링크는 말미에 1개만**(`https://jaylog.co.kr/?p=<id>` 형태, 발행글만). 하루 2~3개까지만(스팸·계정 보호). 붙이기 직전 여전히 미답변인지 확인.
  - 규정·수치는 정확히(면세 150달러·미국 200달러, 합산과세는 '입항일'이 아니라 '같은 판매자+같은날 구매' 기준 등). 초안 작성물은 `research/지식인-답변팩-NN.md`에 기록.

## 🗂️ 환경별 역할
- **폰/클라우드(claude.ai) 등 키 없는 환경**: 글 초안을 아래 형식으로 `drafts/` 폴더에 저장하고 커밋·푸시만 하면 끝. **GitHub Actions(`publish-draft.yml`)가 자동으로 썸네일 생성 → 워드프레스 등록 → 하루 1개 예약**까지 처리한다 (PC 불필요).
- **키 있는 PC**: 위와 동일하게 drafts/ 방식을 써도 되고, 직접 REST API로 등록해도 된다.

## 📝 drafts/ 초안 형식 (클라우드 자동 등록용)
`drafts/글제목-슬러그.md` 파일로 저장. 형식:

```markdown
---
title: 글 제목 (한국어)
slug: english-slug-for-url
tags: [키워드1, 키워드2, 키워드3, 키워드4, 키워드5]
category: 6
schedule: auto   # auto=예약 큐 맨 뒤(하루 1개) / now=즉시 발행 / draft=WP 초안으로만
calculator: none # 관부가세 계산기 — 세금 계산과 관련된 글에만: 일반/의류/가방/건기식/화장품/전자0/전자8, 무관한 글은 none
excerpt: 검색결과에 보일 1~2문장 요약
answer_summary: |
  글의 핵심을 2~3문장으로 압축한 '한눈 요약'. 답변엔진(ChatGPT·Perplexity)이 그대로 인용하기 좋게, 질문에 대한 결론부터. (파이프라인이 본문 맨 위 요약 박스로 렌더 — GEO)
faq:            # 자주 묻는 질문 3개 — 하단에 FAQ 섹션 + FAQPage JSON-LD로 자동 렌더(답변엔진 인용용)
  - q: 독자가 실제로 검색할 법한 질문 1
    a: 2~3문장 답. 결론부터, 정확하게.
  - q: 질문 2
    a: 답 2
  - q: 질문 3
    a: 답 3
thumbnail_brief: |
  최고급 프리미엄 경제지 표지 스타일의 블로그 썸네일. 가로형 구도.
  럭셔리한 다크 네이비 배경, 시네마틱 조명, 금색 입자, 검은 대리석 바닥, 8K 광고 품질.
  금박 질감의 큰 한글 텍스트 '(핵심문구 2~8자)', 흰색 작은 텍스트 '(보조문구)'.
  (글 주제 관련 실사풍 소품 묘사). 실제 브랜드 로고·상표명은 넣지 말 것 (무상표 제품으로).
  한글 텍스트는 정확하게: 문구1 / 문구2
body_image_brief: |
  글의 가장 중요한 핵심 대목을 장면으로 보여주는 본문 이미지. 가로형 실사풍.
  럭셔리한 다크 네이비+골드 톤, 시네마틱 조명 (썸네일과 같은 프리미엄 분위기).
  (핵심 내용을 보여주는 장면 묘사 — 예: 세관 검사대 위 택배상자, 저울 위 영양제 병 6개).
  텍스트는 넣지 말 것 (숫자만 허용). 브랜드 로고 금지.
body_image_alt: 이미지가 보여주는 내용 한 문장 (img alt용)
---
<p>본문 HTML — 글쓰기 규칙(후킹 도입, h2 3~5개, 표/리스트, 내부링크, 고지문) 그대로.</p>
<p>본문 이미지가 들어갈 자리(가장 중요한 핵심 대목)에 마커 한 줄: <!--본문이미지--> (없으면 두 번째 h2 앞에 자동 삽입)</p>
```

## 🧹 기타
- 임시파일은 시스템 임시폴더에만 (프로젝트 폴더 금지), 글 번호 접두어, 작업 후 삭제.
- 유튜브(youtube.com) 접속 금지.
- 새 PC 셋업 절차는 `README.md` 참고.
