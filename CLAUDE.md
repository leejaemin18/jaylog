# CLAUDE.md — 제이로그 작업 규칙 (모든 PC 공통)

이 저장소는 여러 PC와 폰(클라우드)에서 함께 작업한다. 어느 환경의 Claude든 아래 규칙을 따른다.

## 🔄 동기화 (가장 중요)
- **작업 시작할 때**: 반드시 `git pull origin main` 먼저 — 다른 곳에서 한 작업을 받아온다.
- **의미 있는 변경 후**: 즉시 커밋하고 `git push origin main` — 글계획/작성현황 갱신, 새 초안, 규칙 수정 등.
- 푸시 전 **비밀키 스캔 필수**: `secrets.local.md`에 있는 실제 키/비밀번호 문자열(또는 그 일부)이 커밋 내용에 들어있는지 확인하고, 있으면 중단. (스캔할 때 실제 값은 secrets.local.md에서 읽어서 대조 — 규칙 문서에 키 조각을 적지 말 것)
- `secrets.local.md`는 절대 커밋하지 않는다 (.gitignore에 있음).

## 📌 프로젝트 개요
- 블로그: https://jaylog.co.kr (워드프레스) — 해외직구·통관 정보
- 목표: 구글 애드센스 승인 → 수익화
- 글은 Claude가 REST API로 직접 작성·발행 (인증: `secrets.local.md`, 사용자 claude)
- 이 파일 외 상세: `README.md`, `wp-api-config.md`, `글계획.md`, `작성현황.md`

## ✍️ 글쓰기 규칙 (요약 — 상세는 agents/jaylog-writer.md)
- **타깃: 50대** 독자. 쉬운 말, 실생활 사례, 50대 관심 품목(건기식·골프·명품·의약품·여행 등).
- 구조: 후킹 도입(뻔한 도입 금지) → h2 소제목 3~5개(그 자체로 정보가 되게) → 표/리스트 1개 이상 → 요약. 1,500~2,500자.
- 카테고리: **6 (해외직구·통관 가이드) 단 하나만** 사용.
- 키워드(태그) 4~6개 + 발행글 내부링크 + 말미 고지문.
- 규정·수치는 2026년 기준 정확하게, 불확실하면 공식 출처 확인.
- 썸네일: OpenAI gpt-image-2, 고급 프리미엄 스타일(다크네이비+골드), 글당 1장, 한글 오타 육안 검수. (키 없는 환경이면 썸네일 생략하고 초안만 — "썸네일 필요" 표시 남기기)

## 🚦 발행 정책
- **하루 1개만 발행.** 여러 개는 초안으로 쌓고 예약발행(future)으로 분산, 발행 시간은 매일 다르게.
- 예약발행 누락은 GitHub Actions(`wp-cron-ping.yml`)가 매시간 깨워서 보완함.
- 폰/원격 발행·예약: GitHub Actions `blog-command.yml` (Run workflow → status / publish-one / schedule-queue).

## 🗂️ 환경별 역할
- **키 있는 PC** (`secrets.local.md` 채워진 곳): 글 작성→썸네일→워드프레스 등록까지 전부 가능.
- **폰/클라우드(claude.ai) 등 키 없는 환경**: 기획·초안(텍스트)·검수·저장소 정리까지. 초안은 `drafts/` 폴더에 md로 저장 후 커밋 → 키 있는 환경이 이어받아 썸네일+등록.
- 키 없는 환경에서 작업을 마치면, 남은 일(썸네일·등록 등)을 `작성현황.md`에 "이어서 할 일"로 남긴다.

## 🧹 기타
- 임시파일은 시스템 임시폴더에만 (프로젝트 폴더 금지), 글 번호 접두어, 작업 후 삭제.
- 유튜브(youtube.com) 접속 금지.
- 새 PC 셋업 절차는 `README.md` 참고.
